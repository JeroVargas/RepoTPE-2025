<?php require 'app/views/templates/layout/header.phtml' ?>

<main>
    <section id="catalogo" class="product-section">
        <h2>Catálogo de Pisos Disponibles</h2>
        <p class="section-description">
            Explora nuestra selección premium de materiales, incluyendo variedades específicas de mármol y travertino.
        </p>

        <div class="table-container">
            <table class="products-table">
                <thead>
                    <tr>
                        <th>Material</th>
                        <th>Tipo / Variante</th>
                        <th>Origen</th>
                        <th>Acabados Comunes</th>
                        <th>Uso Recomendado</th>
                        <th>Acciones</th> <!-- Nueva columna -->
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($pisos)): ?>
                        <tr>
                            <td colspan="6" style="text-align: center;">No hay pisos cargados en el sistema.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($pisos as $piso): ?>
                            <tr>
                                <td><?= htmlspecialchars($piso->categoria) ?></td>
                                <td><?= htmlspecialchars($piso->tipo_variante) ?></td>
                                <td><?= htmlspecialchars($piso->origen) ?></td>
                                <td><?= htmlspecialchars($piso->acabados_comunes) ?></td>
                                <td><?= htmlspecialchars($piso->uso_recomendado) ?></td>
                                <td class="acciones">
                                    <a href="<?= BASE_URL ?>show-edit-form/<?= $piso->id ?>" class="btn-editar">Editar</a>
                                    <a href="<?= BASE_URL ?>delete-piso/<?= $piso->id ?>" class="btn-eliminar">Eliminar</a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>

                    <!-- Fila vacía con botón "Agregar" -->
                    <tr>
                        <td colspan="5"></td>
                        <td class="acciones">
                            <a href="<?= BASE_URL ?>show-add-piso-form" class="btn-agregar">Agregar</a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>
</main>

<?php require 'app/views/templates/layout/footer.phtml' ?>