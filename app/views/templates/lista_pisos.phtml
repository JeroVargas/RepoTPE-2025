<?php require 'app/views/templates/layout/header.phtml' ?>
<main>
    <section id="catalogo" class="product-section">
        <h2>Catálogo de Pisos Disponibles</h2>
        <p class="section-description">Explora nuestra selección premium de materiales, incluyendo variedades
            específicas de mármol y travertino.</p>

        <div class="table-container">
            <table class="products-table">
                <thead>
                    <tr>
                        <th>Material</th>
                        <th>Tipo / Variante</th>
                        <th>Origen</th>
                        <th>Acabados Comunes</th>
                        <th>Uso Recomendado</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($pisos)): ?>

                        <tr>
                            <td colspan="5" style="text-align: center;">No hay pisos cargados en el sistema.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($pisos as $piso): ?>
                            <tr>
                                <td><?= htmlspecialchars($piso->categoria) ?></td>
                                <td><a href="detalle_piso/<?= $piso->id ?>"><?= htmlspecialchars($piso->tipo_variante) ?></a></td>
                                <td><?= htmlspecialchars($piso->origen) ?></td>
                                <td><?= htmlspecialchars($piso->acabados_comunes) ?></td>
                                <td><?= htmlspecialchars($piso->uso_recomendado) ?></td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </section>
</main>

<?php require 'app/views/templates/layout/footer.phtml' ?>